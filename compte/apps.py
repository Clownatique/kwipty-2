from django.apps import AppConfig


class CompteConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'compte'

'''
Il serait important de savoir si créer une webapp entière 
dédié simplement à la gestion des utilisateurs c intelligent

(moi personellement ça me choque pas)

'''